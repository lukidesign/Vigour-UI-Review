# 源码更新与交付状态

更新：2026-09-24。发布商/版权署名：LukiDesign；产品名：Vigour UI Review。

## 本次整理的边界

2026-09-24 先完成本地说明和验证入口整理，随后准备将阶段 A/B/C 的源码、测试及文档作为开发进展提交同步到 GitHub。此轮不创建 Git tag、不发布 Release、不提交商店审核、不改写实际 Native 注册。源码上传成功与否以 Git 远端提交为准，不等于安装包交付或审核通过。

| 对象 | 可确认的状态 |
| --- | --- |
| GitHub 下载版本 | v0.0.1，2026-08-28 发布，旧版终端启动包；2026-09-24 查询确认 |
| 远端 main | 2026-09-16 隐私政策提交 `02f45496253e7ca6365892265e52360297c50109`；2026-09-24 查询确认 |
| 本地源码 | 版本字段仍为 0.0.1，包含未发布 Native/安装器开发；不是旧下载包的同一内容 |
| 商店条目 | ID `dmkfgmpbomjjhalgnfhcpalobdnklchh`；2026-09-16 最后操作为保存私享草稿和测试说明，未提交审核 |
| 配套安装器 | Apple Silicon 开发构建；无 Developer ID 正式签名/公证，无匹配商店版公开下载交付 |
| Windows 11 x64 | 已确认[独立 SPEC](spec/WINDOWS-BETA-SPEC.md)，本地正在开发安装器、Native 注册与 CI；尚无 Windows 安装包或真机验收结论 |
| 英文 UI | 英文商店文案不代表英文 UI 已实现 |

## 可以作为源码更新说明的内容

- 采集绑定文档和视口、互斥/取消/超时、页面恢复、精确扩展来源校验。
- Native 受限控制协议、按需启动/复用、单次票据、会话级令牌、空闲退出逻辑。
- 图形安装、更新、修复、同结构回退及保留用户数据卸载。
- 工作台平铺/缩放修复、图标、商店素材、隐私说明和 LukiDesign 署名。

完整历史证据见[阶段 A](STAGE-A-REVIEW.md)、[阶段 B](STAGE-B-REVIEW.md)、[阶段 C](STAGE-C-REVIEW.md)。不要改写历史报告来冒充新验证结果。

## 验证记录与缺口

2026-09-24 整理后重新执行 `pnpm release:check`，退出码 0：仓库检查、素材检查、工作区类型检查、121 项 JS/TS 测试、生产构建、11 项 Python 测试及 50 对/500 处合成基准全部通过。另通过 `git diff --check` 和发布脚本语法检查；已逐字节核对本地隐私政策与远端 main 一致。

非阻断提示：工作台 JS 构建体积约 1.50 MB（gzip 约 464 KB），存在大 chunk 提示；Python 提示缺少 ccache。未运行新的 Native/安装器集成、真实 Chrome 采集、干净机器安装或休眠测试，不将基础检查通过等同于内测发布就绪。

2026-09-16 已有 Python 11 项、50 对/500 处合成基准及隔离 Native/安装器验证记录。本轮不据此宣称新机器安装或真实 Chrome 工具栏链路通过。当天独立数据目录中的示例图片分析返回 10 个问题，仅证明本地示例分析可运行。

现有 CI 调用 `release:check`：包含源码、类型、单测、构建、Python、合成基准；本轮将素材检查也加入此入口。**Native/离线安装器集成尚未纳入 CI**，必须另跑，不能以绿色 CI 替代。

开发集成验证入口（macOS Apple Silicon）：

```bash
pnpm release:check
pnpm test:native
# 需先制作并封装新的 companion payload，替换为其实际路径
VIGOUR_INSTALLER_PAYLOAD=/absolute/path/to/sealed-companion pnpm test:installer
```

Native 测试需 Rust 编译器（或设置 `VIGOUR_RUSTC`）。两种集成测试都会使用 4179 端口，必须顺序运行，且端口应空闲；不停止未知进程。安装器测试只操作隔离目录，不能证明普通用户下载后的 Gatekeeper 行为。详细构建入口见[安装说明](INSTALL.md)。

Windows 11 x64 增量在本地源码开发中：已加入受保护凭据帮助程序、随包 Python 启动器、当前用户安装与 Native 注册逻辑、Inno Setup 图形安装脚本和 Windows CI。Mac 上的隔离安装器测试不代表 Windows 运行结果；Windows CI 尚未在本次改动上执行，真实 Chrome、安装器、OCR 模型离线使用及普通用户电脑均未验收。开发构建入口见[Windows 内测说明](WINDOWS-BETA-STATUS.md)。

## 内测安装包发布门槛

- [ ] 统一根项目、各包、扩展 manifest、Python 和服务运行时版本；GitHub beta 标签与商店可接受的数值版本建立明确对应，不将 SemVer 后缀直接写入 manifest.version。
- [ ] 使用商店真实 ID 构建配套安装器；提供扩展/Companion 的版本对应、下载链接、SHA-256、系统要求与安装说明。
- [ ] 在普通 Chrome 完成工具栏点击 → Native 启动 → 视口/整页采集 → 最近采集 → 分析 → 导出；测试取消、切页、导航、重启。
- [ ] 干净 Mac 安装和升级/修复/回退/卸载；记录安全提示，不以关闭安全保护作为验收成功。
- [ ] 真实等待空闲退出，验证后台、多窗口和休眠恢复，不仅依赖模拟时钟。
- [ ] 核对最低 macOS：当前文档目标 14+，安装器声明 13.0，尚需统一并按完整运行链实测。
- [ ] 复核截图、DOM、会话凭据和可选 AI/Figma 的披露；发布者确认合规承诺。
- [ ] 替换审核说明中的版本/下载/实机结果占位，确定测试名单和可用反馈入口，再单独决定是否提审。

真实业务页面盲测仍需人工标注；合成测试不代表生产准确率。1920/1440 桌面已有检查记录，900/720 宽度仍溢出；不要宣称全设备自适应。

## 推送与提交建议

1. 先核对本地与远端隐私政策，保留远端已发布内容；提交基于远端 main 整合，不覆盖隐私政策或强推。
2. 审阅采集安全、Native/安装器与测试、工作台修复、品牌与商店文档。此次文件存在交叉依赖，以完整开发快照提交，避免上传缺少新增文件的半套实现。
3. 快照包含本次状态/变更记录整理及验证入口，明确记录未完成项；后续功能按独立范围提交。
4. 不提交 `.venv`、运行数据库、令牌、用户邮箱、客户截图或约 1 GB 安装产物；产物应在验收通过后另发 Release。
5. 源码可先更新，安装包待验收通过再制作预发布；保留旧 v0.0.1，不覆盖同名版本。

## 本轮对抗式复核（测试前）

- 防止把开发源码称为已发布下载：中英文 README、CHANGELOG 和商店索引分别说明边界。
- 防止把已知 ID 当作已配对交付：只回填事实 ID，保留版本、下载和实机验收门槛。
- 防止隐私文档回退：本轮不修改已发布的 `docs/PRIVACY.md`，推送前须核对远端内容。
- 防止自动测试误占用服务：本轮基础验证不调用 Native/安装器集成，不改写用户 Chrome 或停止预览进程。
- 防止状态报告过时：商店状态注明最后观察日期，不把历史草稿状态冒充当天重新确认。
