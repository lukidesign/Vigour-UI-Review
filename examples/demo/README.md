# Synthetic demo pair

These two images are generated by the deterministic benchmark and contain no real user, customer, or third-party product data.

- `design.png`: upload as the design source.
- `implementation.png`: upload as the Web implementation.

The pair intentionally contains position, size, color, missing, and extra differences. It is intended for a quick functional demonstration, not for measuring production accuracy.
